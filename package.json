{
  "name": "preprocessor-loader",
  "version": "1.0.3",
  "description": "Preprocessor loader for webpack.  Allows for regular expressions, callbacks, macros, similiar to the C preprocessor",
  "main": "preprocessor-loader.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/artificialtrends/preprocess-loader"
  },
  "author": "Jared Spigner",
  "license": "MIT",
  "devDependencies": {
    "fs": "0.0.2",
    "loader-utils": "^0.2.11",
    "path": "^0.12.7"
  }
  "dependencies": {
    "fs": "0.0.2",
    "loader-utils": "^0.2.11",
    "path": "^0.12.7"
  }
}
